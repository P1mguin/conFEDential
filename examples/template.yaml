wandb:
  dataset_name:
  model_name:
simulation:
  batch_size:
  client_count:
  fraction_fit:
  global_rounds:
  learning_method:
    optimizer:
    # Add learning Rate specifics
  local_rounds:
dataset:
  name:
  preprocess_fn:
    # Python function
  splitter:
    alpha:
    percent_noniid:
model:
  criterion:
    type:
  layers:
    # Complete PyTorch model